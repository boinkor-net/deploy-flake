name: "deploy-flake tests"
on:
  workflow_call:

jobs:
  flake_build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.5.3
      - uses: cachix/install-nix-action@v22
      - run: nix build --no-link .#deploy-flake
      - run: nix flake check
